var scrl = Math.max(document.body.scrollTop,document.documentElement.scrollTop);
function scrolling(){
    scrl = Math.max(document.body.scrollTop,document.documentElement.scrollTop);
}
var pxs = $(window).width()/1500;
function pixelsize(){
    pxs = $(window).width()/1500;
    console.log(pxs)
}